# 🏗️ Architecture de Kizuna

Ce document présente l'architecture technique du projet Kizuna de manière visuelle et simplifiée.

## 📊 Diagramme de flux principal

```
┌─────────────────────────────────────────────────────────────────────┐
│                            UTILISATEUR                               │
└────────────────────────────┬────────────────────────────────────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ↓                    ↓                    ↓
┌───────────────┐   ┌───────────────┐   ┌───────────────┐
│   Navigation  │   │  Connexion    │   │   Profil      │
│   (Header)    │   │  (Drawers)    │   │  (Settings)   │
└───────┬───────┘   └───────┬───────┘   └───────┬───────┘
        │                   │                   │
        └───────────────────┼───────────────────┘
                            ↓
        ┌───────────────────────────────────────┐
        │         PINIA STORES (État)           │
        │  ┌─────────────────────────────────┐  │
        │  │  usePocketbaseStore (Central)   │  │
        │  └──────────────┬──────────────────┘  │
        │                 │                     │
        │  ┌──────────────┴──────────────┐      │
        │  │  useMyAuthStore  │  useUserStore   │
        │  └─────────────────┴─────────────────┘ │
        │         (Authentification)             │
        └───────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        ↓                   ↓                   ↓
┌───────────────┐  ┌───────────────┐  ┌───────────────┐
│   PocketBase  │  │   AniList     │  │   LocalStorage│
│   (Backend)   │  │   (GraphQL)   │  │   (Thème)     │
└───────────────┘  └───────────────┘  └───────────────┘
```

## 🗂️ Organisation des dossiers

```
Kizuna/
│
├── app/                    # Code source frontend
│   ├── components/         # Éléments d'interface
│   │   ├── alerts/        │   # Modales de confirmation
│   │   ├── drawers/       │   # Panneaux latéraux (login/register)
│   │   ├── icons/         │   # Icônes SVG
│   │   ├── toast/         │   # Notifications
│   │   ├── header.vue     │   # Barre de navigation
│   │   ├── footer.vue     │   # Pied de page
│   │   ├── profile.vue    │   # Page de profil
│   │   └── scrollToTop.vue│   # Bouton retour haut
│   │
│   ├── composables/        # Stores Pinia
│   │   ├── usePocketbaseStore.ts   # Client PocketBase
│   │   ├── useMyAuthStore.ts       # Authentification
│   │   ├── useAnilistAuthStore.ts  # OAuth AniList
│   │   ├── useUserStore.ts         # Données utilisateur
│   │   ├── useThemeStore.ts        # Thème clair/sombre
│   │   ├── useToastStore.ts        # Notifications toast
│   │   ├── useAlertStore.ts        # Alertes/modales
│   │   └── useDrawersStore.ts      # Gestion drawers
│   │
│   ├── middleware/         # Protection des routes
│   │   └── auth.ts        │   # Vérifie si connecté
│   │
│   ├── pages/             # Pages (routing auto)
│   │   ├── auth/
│   │   │   └── callback.vue  # Callback OAuth
│   │   ├── index.vue      │   # Accueil
│   │   └── profilePage.vue│   # Profil
│   │
│   ├── plugins/           # Initialisation
│   │   └── auth.client.ts │   # Init auth au démarrage
│   │
│   └── app.vue            # Layout principal
│
├── server/                # Code serveur
│   └── api/
│       └── anilist/
│           └── exchange-token.post.ts  # Échange token OAuth
│
├── shared/                # Types partagés
│   └── types/
│       ├── UserType.ts    │   # Interface utilisateur
│       ├── NewUserType.ts │   # Nouvel utilisateur
│       ├── AlertType.ts   │   # Type alerte
│       └── ToastType.ts   │   # Type toast
│
└── docs/                  # Documentation
    └── README.md          │   # Guide complet
```

## 🔄 Cycle de vie d'une requête

### 1. Démarrage de l'application

```
┌─────────────┐
│   Browser   │
└──────┬──────┘
       │ GET /
       ↓
┌─────────────────────────────────┐
│  Nuxt Server (SSR si activé)    │
│  ┌───────────────────────────┐  │
│  │  1. auth.client.ts        │  │
│  │     → authRefresh()       │  │
│  │     → setTheme()          │  │
│  └───────────────────────────┘  │
└──────────────┬──────────────────┘
               │
               ↓
┌─────────────────────────────────┐
│  Vue App montée avec :          │
│  - État auth initialisé         │
│  - Thème appliqué               │
│  - Layout (Header + Footer)     │
└─────────────────────────────────┘
```

### 2. Connexion utilisateur

```
┌──────────┐     ┌────────────┐     ┌──────────────┐
│  User    │────▶│  Drawer    │────▶│ useMyAuth    │
│  clique  │     │  Login     │     │ Store        │
└──────────┘     └────────────┘     └──────┬───────┘
                                          │
                                          │ pb.authWithPassword()
                                          ↓
                                   ┌──────────────┐
                                   │  PocketBase  │
                                   └──────┬───────┘
                                          │
                                          │ token + record
                                          ↓
                                   ┌──────────────┐
                                   │  Stores mis  │
                                   │  à jour      │
                                   └──────┬───────┘
                                          │
                                          │ Toast + Redirect
                                          ↓
                                   ┌──────────────┐
                                   │  Dashboard   │
                                   └──────────────┘
```

### 3. Liaison compte AniList

```
┌──────────┐     ┌────────────┐     ┌──────────────┐
│  User    │────▶│ useAnilist │────▶│ AniList.co   │
│  clique  │     │ AuthStore  │     │ (OAuth)      │
└──────────┘     └────────────┘     └──────┬───────┘
                                          │
                                          │ code → /auth/callback
                                          ↓
                                   ┌──────────────┐
                                   │ callback.vue │
                                   └──────┬───────┘
                                          │
                                          │ handleCallback()
                                          ↓
                                   ┌──────────────┐
                                   │ /api/anilist/│
                                   │ exchange-token│
                                   └──────┬───────┘
                                          │
                                          │ access_token
                                          ↓
                                   ┌──────────────┐
                                   │ GraphQL      │
                                   │ aniList.co   │
                                   └──────┬───────┘
                                          │
                                          │ User data
                                          ↓
                                   ┌──────────────┐
                                   │ PocketBase   │
                                   │ user.update()│
                                   └──────┬───────┘
                                          │
                                          │ Toast succès
                                          ↓
                                   ┌──────────────┐
                                   │ Profil lié ! │
                                   └──────────────┘
```

## 🧩 Interactions entre stores

```
                    ┌─────────────────────┐
                    │ usePocketbaseStore  │
                    │  - pb (client)      │
                    │  - authRecord       │
                    │  - authToken        │
                    └──────────┬──────────┘
                               │
         ┌─────────────────────┼─────────────────────┐
         │                     │                     │
         ↓                     ↓                     ↓
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│  useMyAuthStore │  │  useUserStore   │  │ useAnilistStore │
│  - login()      │  │  - userData     │  │  - loginWith    │
│  - logout()     │  │  - saveUser()   │  │    AniList()    │
│  - createAcct() │  │  - updateUser() │  │  - handle       │
└─────────────────┘  └─────────────────┘  │    Callback()   │
                                          └─────────────────┘
         │                     │                     │
         └─────────────────────┼─────────────────────┘
                               │
         ┌─────────────────────┼─────────────────────┐
         │                     │                     │
         ↓                     ↓                     ↓
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│ useThemeStore   │  │ useToastStore   │  │ useAlertStore   │
│ - setTheme()    │  │ - openToast()   │  │ - openAlert()   │
│ - changeTheme() │  │                 │  │                 │
└─────────────────┘  └─────────────────┘  └─────────────────┘
```

## 🎨 Flux de rendu d'un composant

```
┌─────────────────────────────────────────────────────────┐
│  Composant Vue (ex: header.vue)                         │
├─────────────────────────────────────────────────────────┤
│  1. <script setup>                                      │
│     └─→ Import des stores                               │
│     └─→ Computed properties (réactif)                   │
│     └─→ Méthodes (actions)                              │
├─────────────────────────────────────────────────────────┤
│  2. <template>                                          │
│     └─→ Structure HTML                                  │
│     └─→ Directives Vue (v-if, v-for, v-model)           │
│     └─→ Classes Tailwind CSS                            │
├─────────────────────────────────────────────────────────┤
│  3. Rendu                                               │
│     └─→ Vue compile le template                         │
│     └─→ Mise à jour réactive quand les stores changent  │
└─────────────────────────────────────────────────────────┘
```

## 🔐 Sécurité

### Authentification

```
┌──────────────────────────────────────────────────────────┐
│  Flux de sécurité                                        │
├──────────────────────────────────────────────────────────┤
│  1. Mot de passe haché par PocketBase (bcrypt)           │
│  2. Token JWT stocké dans localStorage                   │
│  3. Middleware auth.ts protège les routes                │
│  4. Token rafraîchi automatiquement                      │
└──────────────────────────────────────────────────────────┘
```

### OAuth AniList

```
┌──────────────────────────────────────────────────────────┐
│  Sécurité OAuth                                          │
├──────────────────────────────────────────────────────────┤
│  1. state parameter (protection CSRF)                    │
│  2. client_secret côté serveur uniquement                │
│  3. HTTPS requis pour redirect_uri                       │
│  4. Token stocké dans PocketBase (chiffré)               │
└──────────────────────────────────────────────────────────┘
```

## 📈 Performance

### Optimisations implémentées

| Technique | Description | Bénéfice |
|-----------|-------------|----------|
| **Pinia** | État global réactif | Pas de props drilling |
| **Computed** | Valeurs calculées en cache | Évite les recalculs |
| **Lazy loading** | Pages chargées à la demande | Réduit le bundle initial |
| **Tailwind** | CSS utilitaire purgé | CSS minimal en prod |
| **Vite** | Bundler moderne | HMR instantané |

---

*Cette architecture permet une application maintenable, évolutive et performante.*
